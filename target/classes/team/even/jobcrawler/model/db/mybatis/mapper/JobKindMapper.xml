<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.even.jobcrawler.model.db.mybatis.mapper.JobKindMapper">
	<insert id="insertJobKind" parameterType="JobKind">
		insert into jobkind values #{kind}
	</insert>
	
	<select id="selectAll" resultType="JobKind">
		select * from jobkind
	</select>
</mapper>